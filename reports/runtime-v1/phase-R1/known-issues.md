# Phase R1 Known Issues

## Blocking exit evidence

Local Docker API access is unavailable; see the dated blocker report. GitHub Actions is configured to supply equivalent image-build/Compose/readiness evidence after the R1 push.

## Assigned future scope

- R2 adds official MCP Streamable HTTP, Registry validation and immutable operation snapshots.
- R3 adds authoritative Task migrations/repositories/lifecycle.
- R7 enforces production mTLS, auth, metrics and trace behavior.
- R8 completes durable, behavior-rich TypeScript/Python Adapters.

These are explicit phase assignments, not claimed R1 functionality or release waivers.
