{
  "apiVersion": "v1",
  "kind": "Service",
  "metadata": {
    "name": "sdar-runtime",
    "namespace": "sdar-system",
    "labels": { "app.kubernetes.io/name": "sdar-runtime" }
  },
  "spec": {
    "type": "ClusterIP",
    "selector": { "app.kubernetes.io/name": "sdar-runtime" },
    "ports": [{ "name": "http", "port": 8080, "targetPort": "http", "protocol": "TCP" }]
  }
}
